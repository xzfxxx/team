---
name: team-mode
description: 触发团队开发模式：主agent仅调度，从角色模板库生成开发/测试/架构/质量等角色并协作。按任务类型选/生成工作流模板（标准开发/数据映射/修bug/前端/调研/紧急）。支持 DSH、Claude Code、Codex、Gemini、Cursor、Roo 多平台自动适配，平台检测+调用说明本会话只加载一次。先做复杂度门：简单任务自动降级/拒绝（不硬开团队）；复杂任务即使未显式说"/团队"也建议启用。角色库支持临时角色，完成后可沉淀为永久。用户说"/团队"、"/team"、"/dev"时触发。
---

# 团队开发模式（多平台便携版）

> **核心原则：主 agent 仅调度，不写码、不验收。所有编码和验收由独立子 agent 执行。**

## 🔍 复杂度门（最先执行）

先判值不值得开团队，用 `ask_user_question` 让用户拍板：
- **简单**（单点小改/需求清晰/低风险）→ 不硬开。降级 `templates/quick-task.md`（单 agent + 质量抽查），或直接说明「任务简单，团队过重」让用户选：直接执行 / 仍要团队。
- **复杂**（多模块/跨角色/高风险/需独立验收/改数据库）→ 进入团队模式。
- **已说 / 团队 但简单** → 主动提示"无需团队"，建议降级。
- **没说 / 团队 但较复杂** → 主动建议"此任务较复杂，建议 / 团队 开启团队模式，确认吗？"（即便本 skill 因复杂度被自动想起，也先确认再开）。

判断依据：改动文件数、是否跨模块/角色、是否涉 SQL/权限/数据、是否需要独立验收兜底。

**① 成本感知（多 agent 是 15× token，先算账再开）：**
- 单 agent ≈ 1×，带工具 ≈ 4×，**多 agent ≈ 15×**——只有**隔离/并行收益 > 开销**才值得。
- 判据：任务够大、能明显并行、跨角色隔离收益高 → 开；任务小、可串行、单人能连贯做完 → 降级单 agent。
- 一句话：**"不是所有复杂都该开团队，先问值不值。"** token 上去了，正确率不如直接换个好模型（升级模型往往比堆 token 划算）。

## 🔍 平台检测（本会话只加载一次）

看本会话前面是否已有标记 `[团队模式] 平台 = X`：
- **有** → 复用 X，**跳过检测、跳过加载平台说明**。
- **无** → 检测（按序命中即停）：`subagent/subagent_fork/workflow`→DSH；**DSH 且另有 `team_spawn`/`team_send`（已装 dsh-team 插件）→ DSH+dsh-team（优先用它的 team 工具）**；`Agent`(大写)→Claude Code；`codex` CLI→Codex；`gemini` CLI/`[subagent]`→Gemini CLI；Cursor 编辑器→Cursor；Roo Code→Roo Code；都没有→DSH。

写入标记 `[团队模式] 平台 = X（已检测，后续不再检测/加载说明）`，再**按平台读取一次**对应 `references/<platform>.md`。DSH+dsh-team 读 `references/dsh-team-platform.md`。

> 角色、模板跨平台完全复用（prompt/文件驱动），只有"子代理调用方式"按平台看 `references/`。

## 💡 角色模板库

需要角色 → 读 `roles/<角色>.md` → 把 `【Persona】` 注入子代理 prompt 开头，再拼任务。

| 角色 | 文件 | 用途 |
|------|------|------|
| 产品经理 | `roles/产品经理.md` | 需求澄清 / 验收标准 |
| 架构师 | `roles/架构师.md` | 架构设计 / 依赖分析 / 选模板 |
| 开发工程师 | `roles/开发工程师.md` | 编码 + 单元测试 |
| 测试工程师 | `roles/测试工程师.md` | 测试用例设计 + 执行 |
| 数据库工程师 | `roles/数据库工程师.md` | SQL 优化 / HANA / MyBatis（默认只读） |
| 前端工程师 | `roles/前端工程师.md` | 页面 / 组件 / 交互 |
| 质量工程师 | `roles/质量工程师.md` | 独立验收 |
| 文档工程师 | `roles/文档工程师.md` | 正式文档 / 知识沉淀 |

**🎭 临时角色**（库不必固定，奇葩业务随时造）：库外角色临时创建，架构师在阶段序列加 `临时角色：<名>` 并写一段 persona（职责+工具导向+交付要求）注入，**不建文件也能跑**（如网络安全/运维/性能优化师）。跑完用 `ask_user_question` 问「是否有用？是否永久加入 roles/？」——是则按模板格式写 `roles/<角色名>.md`，否则丢弃。约束：临时角色同样走"阶段=角色+任务"；编码/验收角色遵守主 agent 只调度边界与工具导向。

## 🗂️ 工作流模板库

模板决定**用哪些角色、按什么阶段走**；读 `templates/<模板>.md` 拿阶段细节。

| 模板 | 文件 | 适用 | 主要接线角色 |
|------|------|------|-------------|
| 标准功能开发 | `templates/standard-dev.md` | 常规功能新增/修改 | 产品→架构→开发→质量(+测试) |
| 数据/HANA+MyBatis生成 | `templates/data-mapper.md` | 表结构/SQL/生成器/映射 | 数据库→架构→开发+数据库→质量→测试 |
| 修 bug/重构 | `templates/bugfix-refactor.md` | bug/重构/技术债，行为不变 | 开发→架构→开发→测试→质量 |
| 前端页面 | `templates/frontend-page.md` | 页面/组件/交互/联调 | 产品→前端→开发(联调)→测试→质量 |
| 纯查询/分析调研 | `templates/research-query.md` | 只查不改/调研/出结论 | 架构→数据库→主agent |
| 紧急小任务 | `templates/quick-task.md` | 单点小改/时间紧 | 主agent→质量抽查 |

## 使用方式

- 触发：`/团队`、`/team`、`/dev`
- 验收默认 10 轮，用完用询问工具问是否增加

---

# ⚙️ 通用底座（所有模板共用）

## 补充逻辑处理
任何阶段发现需补充逻辑，先暂停、确认清楚再继续。4 场景：① 需求/规划遗漏→列点问用户→更新；② 开发中发现→开发汇报→主 agent 转述→确认后更新 prompt；③ 验收不通过缺逻辑→问题清单→新开发修复时写明→再验收；④ 报告后补充→评估影响→新开发补→单独验收→更新报告。

**汇报格式：**
```
**发现位置**：[文件名:行号](路径) 或 Phase X
**补充内容**：XXX  **原因**：XXX  **是否影响已完成部分**：是/否
```

## 团队规模上限（按独立任务数）
| 任务数 | 总人数 | 说明 |
|--------|-------|------|
| 1 | 3 | ✅ 推荐 |
| 2 | 5 | ✅ 推荐 |
| 3 | 7 | ✅ 最大上限 |
| 4+ | 9+ | 🚫 压缩或分波次 |

**任务数 > 3 提醒：** 检测到超大型任务（X 个，超上限）。建议①压缩合并给现有 3 组，②分波次（第一波并行 3 个）。上限 3 任务（7人）。请确认。

## 模型选择（DSH 可用；其他平台用默认）
| 任务 | 模型 |
|------|------|
| 简单 CRUD | mimo-v2.5 |
| 复杂业务逻辑 | mimo-v2.5-pro |
| 架构级改动 | deepseek-v4-pro |

## 质量验收维度（11 项）
需求完整性；代码质量（命名/结构/可读性）；SQL 语法；MyBatis 映射一致；参数校验/空指针；异常处理/事务边界；日志充分；N+1 查询；安全漏洞（SQL注入/XSS/敏感信息/权限校验）；代码复杂度（方法≤50行/嵌套≤4/圈复杂度≤10）；测试覆盖（正常/边界/异常）。

## 交叉评审（高风险任务，显著提升正确率）
11 维验收之外，**高风险/关键任务加一层独立交叉评审**（生成-判别/辩论）：再派生一个**独立的质量工程师**（与第一个质量师不同、与开发隔离）复核第一个质量师的问题清单，**专门找它漏判的点**（两个质量师互相挑错）。规则：
- 触发：跨模块/改库/权限/金额/上线关键路径才做；普通任务不必。
- 两个质量师**各自独立出具结论**，主 agent 合并两份清单去重后作为修复依据。
- 分歧点由主 agent（或第三质量师）裁决，或标出「争议」交给用户。
- 目的不是多一个人，而是**对抗式交叉审查**降低单点漏判。

**收敛防线（在交叉评审上再盯几件事）：**
- **卡死哨兵**：某阶段连续无新进展（如修复不收敛、讨论原地打转）→ 触发提示，拆任务或换方案。
- **模仿/顺从哨兵**：两个质量师结论趋同到没有各自独立依据、或互相背书 → 提示重审，防 sycophancy。
- **跑偏哨兵**：某队友目标与整体偏离 → 停下对齐目标，必要时收敛检查。
- **加权裁决**：多个质量师结论冲突时，按**置信度/专长加权**（不是简单多数），并以独立第三意见裁决；仍分歧 → 标「争议」交用户。

## 角色产物契约（#4 交接像外包合同）
每个角色有**明确输入 + 输出产物**，流水线据此交接、可单独验收。主 agent 按此契约派活：

| 角色 | 输入产物 | 输出产物 |
|------|---------|---------|
| 产品经理 | 原始需求 | 需求要点 + 验收标准 |
| 架构师 | 需求/模块 | 工作流 + 依赖 + 改动面 |
| 开发工程师 | 任务 + 设计 | 代码 + 单测 |
| 数据库工程师 | 表/元数据（只读） | SQL/映射 + 优化建议 |
| 测试工程师 | 代码 + 需求 | 测试用例 + 结果 |
| 质量工程师 | 代码 + 需求/契约 | 问题清单（分级） |
| 文档工程师 | 全部结果 | 文档 + 知识沉淀 |

> 交接原则：**上游交付的产物是下游的明确输入**，缺哪个就退回哪个（不越界代做）；每份产物可独立验收。

## 防电话游戏直通（② 别让主 agent 二次转述丢细节）
主 agent 二次转述会丢信息（"电话游戏"），所以：
- **直通**：角色产出的**面向用户的最终结论**（验收结论、文档、最终报告）由**该角色直接呈现给用户**，不要主 agent 再概括一遍；多团队场景主 agent 只按小组聚合一次。
- **浓缩摘要**：worker 回传给主 agent 时返回**浓缩摘要**（distill），只留结论+关键位置，不塞全量细节——避免主 agent 上下文爆炸。
- 一句话：**"直通能到用户的就别转述，要转的就浓缩再转。"**

## 错误恢复策略（开发）
| 失败类型 | 恢复方式 |
|----------|----------|
| 编译错误 | 新开发，附错误信息 |
| 测试失败 | 新开发，附失败用例+堆栈 |
| 超时/无响应 | 拆更小粒度 |
| 连续失败 2 次 | 汇报用户，请求介入 |

## 修复循环 + 轮数
验收不过：主 agent 收问题清单 → **把清单发给同一个开发队友**（不是新开的）→ 修复 → 再验收 → 循环。**默认 10 轮**，用完问是否 +10，用户拒绝才标记失败。轮数：`3/10` → `11/20`。

**常驻队友（关键）**：每个角色队友 **spawn 一次、跨轮次复用**，修复/复审用 `send_message`/`team_send` 发给**同一个队友**，不要为新轮次另起新子代理——它保留自己写过的代码上下文，第二次修更准。DSH：subagent 是 continuable，spawn 后拿到 id，后续复用；装了 dsh-team 则用 `team_send`。

## 进度可见性（主 agent 维护小组状态表）
```
| 小组 | 任务 | 状态 | 当前轮数 | 说明 |
```
状态：`⏳等待中 / 🚀执行中 / 🔍验收中 / 🔧修复中 / ✅已完成 / ❌失败`。子代理用 `log()` 实时汇报。

## 最终汇报（由谁产出）
- **默认主 agent 直接总结**（已汇总各角色结果，最准最省，不开多余子代理）。
- **正式文档/README/知识库** → 交给 `文档工程师`。
- **结论 → 知识沉淀**：写记忆（DSH MCP memory / Claude CLAUDE.md）。

**格式：**
```
## 团队协作 - 最终报告
**任务概览：** X 个任务，成功 X，失败 X
### 小组1 - <模块>
**结论：** 已完成  **改动位置：** [File.java](src/...):45-120  **测试情况：** ...  **未解决问题：** 🟡 [File:行号] ...
```

---

# ▶️ 流程编排（模板驱动）

## Phase 1：需求澄清（产品经理，若模板含）
1. 整理需求要点，列出理解的功能点
2. 问不明处，列出待确认问题
3. 等用户确认后再进下一阶段

## Phase 2：流程规划（架构师选模板 / 新规划工作流 + 依赖分析）

**现有模板可能覆盖不了所有场景；覆盖不了就【新规划一条工作流】——与"选模板"同级，不是兜底。** 架构师读 `templates/`，决策：
1. **读模板库**：看现有模板能否覆盖整个任务。
2. **完全覆盖** → 读该 `templates/<模板>.md`，以其阶段序列为框架。
3. **部分覆盖** → 模板作主体 + 自定义补阶段。
4. **不覆盖/复合新场景** → **架构师新规划一条工作流**。

**新规划工作流的规则：**
- 阶段序列 = 角色 + 任务（如 `PM→架构→数据库→开发→测试→质量→报告`）。
- 角色优先取自角色库（8 个之一），可复用、可多角色并行；库外需要时用**临时角色**。
- **必须含「验收」阶段**（质量 11 维/临时验收角色）+ **「报告/知识沉淀」**。
- 复用通用底座：补充逻辑、规模上限、修复循环/轮数、进度表、模型选择。
- 拿不准某阶段 → 读对应 `roles/<角色>.md` 拿 persona 与交付要求。

**规划输出：** ① 任务分析（独立/依赖）② 工作流（`模板：XXX` 或 `新规划：自定义` + 完整阶段序列）③ 小组编排（并行/串行 + 规模）④ 风险与边界。给出确认，通过后按此执行。

**知识反馈：** 若"新规划"场景以后会重复，架构师把它沉淀成新模板写入 `templates/`（下次直接选）。

**确认输出格式：**
```
## 任务分析
**独立任务：** 1. 任务A ... 2. 任务B ...
**依赖任务：** 3. 任务C（依赖任务A）→ 等A完成
## 工作流（模板：标准功能开发 / 或 新规划：自定义）
Phase 1 产品经理: ...  Phase 2 架构师: ...  Phase 3 数据库工程师: ...
## 小组编排
**第一波（并行）：** 任务A + 任务B  **小组规模：** 5人（1项目经理 + 2开发 + 2验收）
请确认流程？
```

## Phase 3+：按所选模板 / 新规划工作流执行
- **主 agent 只调度，禁止写码/验收。**
- **常驻队友**：每个角色队友 spawn 一次就留着，后续轮次（修复/复审）复用同一个，不每轮重开——保留上下文，第二次更准。
- **上下文隔离档位（按任务给队友喂多少，省 token 的关键）**：
  - 简单/边界清晰子任务 → **只传指令**（任务 + 要点 + 产物契约），让队友 lean 运行。
  - 复杂/需全局理解 → **全量委托**（共享相关上下文）。
  - 共享状态（如表结构、接口约定）→ 走**文件/看板记忆**，不靠上下文传递。
  - 原则：**隔离上下文才是多 agent 的目的**，角色只是分工；能 lean 就别全量喂。
- 每阶段（模板或新规划）：确定角色 → 读 `roles/<角色>.md` → 注入【Persona】→ 按平台调用（见 `references/`）。
- 并行任务用 workflow/parallel；有依赖则串行。
- 阶段间按通用底座应用修复循环、轮数、进度表、补充逻辑。

## Phase 6：最终报告
按「通用底座 → 最终汇报」：主 agent 总结为主，正式文档交文档工程师，结论沉淀到记忆。
